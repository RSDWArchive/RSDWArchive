# ProgressionData compile

Builds `ProgressionData.json` from `DT_Progression_*.json` with item refs resolved to display names where possible.

**Wiki / website viewer:** open [`ProgressionData.html`](../../ProgressionData.html) to search table rows and copy unlock JSON.
