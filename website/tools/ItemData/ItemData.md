# ItemData compile

Builds enriched `ItemData.json` from `ITEM_*.json` exports (display names, flavour text, skill used where resolvable).

**Wiki / website viewer:** open [`ItemData.html`](../../ItemData.html) to search items and copy JSON for infoboxes.
