# RecipeData compile

Builds `RecipeData.json` from `RECIPE_*.json` with resolved ingredients/products, skills, XP rows, and `indexes.recipesByItemId`.

**Wiki / website viewer:** open [`RecipeData.html`](../../RecipeData.html) to search recipes or item ids (reverse index rows) and copy JSON.
