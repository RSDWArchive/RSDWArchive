# Reports Reminder

Before running:

Ensure 1_generate_report_data.bat is pointing at the new and old versions.

Then run:

- `1_generate_report_data.bat` with option 5

This will generate all reports and run the two addition .py scripts.